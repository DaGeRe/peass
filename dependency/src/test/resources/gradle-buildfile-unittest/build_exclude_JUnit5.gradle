plugins {
	id 'java'
    id 'jacoco'
    id 'org.sonarqube'
}

configurations {
    all {
        exclude group: 'org.junit.vintage', module: 'junit-vintage-engine'
    }
}

test {
    useJUnitPlatform()
}
